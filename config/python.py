import config.project

package_name = config.project.project_name

install_requires = [
    "flask",  # for a web framework
]
dev_requires = [
    "pydmt",
    "pymakehelper",
]

python_requires = ">=3.9"
