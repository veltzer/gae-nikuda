config_requires = []
dev_requires = []
install_requires = [
    "flask",
]
build_requires = [
    "pymakehelper",
    "pydmt",
]
test_requires = []
requires = config_requires + install_requires + build_requires + test_requires
